Step 1: we need to install stream lit package
-pip install streamlit

Step 2:Generally .py files will run by using
       python filename.py

       but streamlite python files should run by below command
       "python -m streamlit run filename.py"